
/* Located in resources so it is not visible anywhere inside the test class path. Compiled manually with javac into a binary file placed in the same directory.*/
public interface CustomCompiledStringMapperInterface {
    public String map(String string);
}